The purpose of this project is to provide a place where my classmates can upload their HTML pages.
Currently, provides functionality to allow the user to upload an HTML page and browse all uploaded 
HTML pages.

to run: 
python3 manage.py runserver
then, in browser:
localhost:8000
